# Copy this to `.env` on the server (never commit the real one) and fill in
# real values. Used by docker-compose.prod.yml.

DATABASE_USERNAME=rolliq
DATABASE_PASSWORD=change-me-to-a-real-random-password

# Generate a real one, e.g.: openssl rand -base64 48
JWT_SECRET=change-me-to-a-long-random-secret
