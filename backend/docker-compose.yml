services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: rolliq
      POSTGRES_USER: rolliq
      POSTGRES_PASSWORD: rolliq
    ports:
      - "5432:5432"
    volumes:
      - rolliq-postgres-data:/var/lib/postgresql/data

volumes:
  rolliq-postgres-data:
